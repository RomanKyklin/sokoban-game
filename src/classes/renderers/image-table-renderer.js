import {BgTableRenderer} from "./bg-table-renderer.js";

export class ImageTableRenderer extends BgTableRenderer {
    constructor(rootElementSelector, playingField) {
        super(rootElementSelector, playingField);
    }
}
